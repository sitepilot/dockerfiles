# Should be included for most sites, as contains sensible defaults
# for file exclusions, security and static file caching.

# Exclusions
include /opt/runtime/config/nginx/server/exclusions.conf;

# Security
include /opt/runtime/config/nginx/server/security.conf;

# Static Content
include /opt/runtime/config/nginx/server/static-files.conf;